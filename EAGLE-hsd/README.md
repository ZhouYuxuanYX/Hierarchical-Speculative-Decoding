# EAGLE with HSD in GSM8k 

To evaluate the performance of EAGLE with HSD in GSM8k, we replace the tokenwise verifier in EAGLE with HSD.

Install the environment as following:

```bash
pip install -r requirements.txt
```

Run it within following code:

```bash
bash 1114_eagle_eval.sh
```